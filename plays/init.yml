---
# Core platform initialization. Artifact publication and pipeline setup are
# explicit opt-ins through their phase playbooks.
- ansible.builtin.import_playbook: init_bootstrap.yml
- ansible.builtin.import_playbook: init_platform.yml
