# server block for nginx
server {
    listen 80;
    listen 443 ssl;
    server_name {{ nginx_acme_label }};

    ssl_certificate /etc/nginx/ssl/ryuugu.dev/full.pem;
    ssl_certificate_key /etc/nginx/ssl/ryuugu.dev/key.pem;
    ssl_trusted_certificate /etc/nginx/ssl/ryuugu.dev/ca.pem;

    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers HIGH:!aNULL:!MD5;

    root /www/data;

    location / {
        autoindex on;
        try_files $uri $uri/ =404;
    }

}

server {
    listen 80;
    server_name {{ openbao_acme_label }};
    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl;
    server_name {{ openbao_acme_label }};

    ssl_certificate /etc/nginx/ssl/ryuugu.dev/full.pem;
    ssl_certificate_key /etc/nginx/ssl/ryuugu.dev/key.pem;
    ssl_trusted_certificate /etc/nginx/ssl/ryuugu.dev/ca.pem;

    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers HIGH:!aNULL:!MD5;

    location / {
        proxy_pass http://openbao:8200;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }

}

server {
    listen 80;
    server_name {{ openbao_api_fqdn }};
    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl;
    server_name {{ openbao_api_fqdn }};

    ssl_certificate /etc/nginx/ssl/ryuugu.dev/full.pem;
    ssl_certificate_key /etc/nginx/ssl/ryuugu.dev/key.pem;
    ssl_trusted_certificate /etc/nginx/ssl/ryuugu.dev/ca.pem;

    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers HIGH:!aNULL:!MD5;

    location / {
        proxy_pass http://openbao:8200;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        proxy_read_timeout 900s;
        proxy_send_timeout 900s;
    }
}

server {
    listen 80;
    server_name {{ concourse_fqdn }};
    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl;
    server_name {{ concourse_fqdn }};

    ssl_certificate /etc/nginx/ssl/ryuugu.dev/full.pem;
    ssl_certificate_key /etc/nginx/ssl/ryuugu.dev/key.pem;
    ssl_trusted_certificate /etc/nginx/ssl/ryuugu.dev/ca.pem;

    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers HIGH:!aNULL:!MD5;

    location / {
        proxy_pass http://{{ concourse_ipv4 }}:8080;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }

}

server {
    listen 80;
    server_name {{ registry_fqdn }};
    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl;
    server_name {{ registry_fqdn }};

    ssl_certificate /etc/nginx/ssl/ryuugu.dev/full.pem;
    ssl_certificate_key /etc/nginx/ssl/ryuugu.dev/key.pem;
    ssl_trusted_certificate /etc/nginx/ssl/ryuugu.dev/ca.pem;

    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers HIGH:!aNULL:!MD5;

    client_max_body_size 5G;

    location / {
        proxy_pass http://{{ registry_ipv4 }}:5000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }

}

server {
    listen 80;
    server_name {{ stigmergy_fqdn }};
    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl;
    server_name {{ stigmergy_fqdn }};

    ssl_certificate /etc/nginx/ssl/ryuugu.dev/full.pem;
    ssl_certificate_key /etc/nginx/ssl/ryuugu.dev/key.pem;
    ssl_trusted_certificate /etc/nginx/ssl/ryuugu.dev/ca.pem;

    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers HIGH:!aNULL:!MD5;

    location / {
        proxy_pass http://stigmergy:8080;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
