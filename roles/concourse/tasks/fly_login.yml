- name: Log in to Concourse
  ansible.builtin.shell: |
    fly -t {{ concourse_target }} login \
      -c {{ concourse_url }} \
      -u {{ concourse_user }} \
      -p {{ lookup('env', 'CONCOURSE_PASSWORD') }}
  delegate_to: localhost
