[Unit]
Description=Set current boot option to next boot option

[Service]
Type=oneshot
RemainAfterExit=true
ExecStart=/bin/bash -c "sudo efibootmgr -n$(efibootmgr | grep BootCurrent | sed -e 's/BootCurrent://')"

[Install]
WantedBy=multi-user.target